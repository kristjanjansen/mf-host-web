# Web host for microfrontends

This a microfrontend host application that consumes remote microfrontends using custom elements.

## Setup

Copy `.env.example` to `.env` and then

```bash
npm install
npm run dev
```

.
